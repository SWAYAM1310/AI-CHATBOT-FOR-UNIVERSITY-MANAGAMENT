"""SQLAlchemy ORM models.

Empty for now — the authoritative schema (derived from data/synthetic/sample/*.csv
headers, not the stale plan.md §2) lands in the next part. Import every model module
here once they exist so Alembic autogenerate sees them.
"""
